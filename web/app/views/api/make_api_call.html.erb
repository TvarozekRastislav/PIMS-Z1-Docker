<h1>Záznam hodov</h1>
<%filtered_array = @response_body.index_by { |hash| hash["throw_id"] }.values %>
<table>
  <thead>
  <tr>
    <th>Throw id</th>
    <th>Destroy link</th>
    <th>Show link</th>
  </tr>
  </thead>
  <tbody>
  <% filtered_array.each do |response| %>
    <tr>
      <td><%= response["throw_id"] %></td>
      <td><%= link_to 'Destroy', destroy_throw_path(id:response["throw_id"]) %></td>
      <td><%= link_to 'Show', show_throw_path(id:response["throw_id"]) %></td>
    </tr>
  <% end %>
  </tbody>
</table>
<%= link_to "Index page", index_path %>

<style>
    /* Define your CSS styles here */
    h1 {
        text-align: center;
        font-size: 24px;
        color: #333;
    }

    table {
        width: 100%;
        border-collapse: collapse;
        margin: 20px 0;
    }

    th, td {
        border: 1px solid #ddd;
        padding: 8px;
        text-align: left;
    }

    th {
        background-color: #f2f2f2;
    }

    tr:hover {
        background-color: #f5f5f5;
    }
</style>